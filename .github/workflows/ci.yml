name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install build and development dependencies
        run: |
          python -m pip install --upgrade pip build
          python -m pip install -e ".[dev,governance]"
      - name: Ruff SDK and tests
        run: ruff check src/cutover tests
      - name: Mypy stable SDK contracts
        run: >-
          mypy
          src/cutover/contracts/intake.py
          src/cutover/telemetry/events.py
          src/cutover/economics/budget.py
          src/cutover/cli.py
      - name: Pytest
        run: pytest -q
      - name: Build distributions
        run: python -m build
      - name: Verify wheel installation
        run: |
          python -m venv /tmp/package-smoke
          /tmp/package-smoke/bin/pip install dist/*.whl
          /tmp/package-smoke/bin/python -c "import cutover"
